[1]

Write a CPP program to concatenate three strings using Operator Overloading.

Note:- Create a class, in which you have to take inputs from user and as a output you have to concatenate the strings using Operator Overloading.